# --- Arduino-CLI Build Artifacts ---
#pnpm support
/node_modules

# Default build directory generated by 'arduino-cli compile --build-path'
build/
# Alternative local build directories
Build/
_build/

# --- Compiled Binaries & Hex files ---
# Ignore final compiled outputs if saved to the sketch directory
*.hex
*.elf
*.bin
*.eep
*.lst
*.map

# --- Local Configuration ---
# Ignore your locally generated profile or specific environment configurations
arduino-cli.yaml
.arduino-cli.yaml

# --- Editor and IDE Metadata ---
# VS Code directories (if using the Arduino/C++ extension)
.vscode/
.clang-database/
compile_commands.json

# --- OS & Temporary Files ---
.DS_Store
Thumbs.db
*.tmp
*.bak

firmware@arduino.ino
